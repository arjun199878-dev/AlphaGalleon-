# 2026-02-19

[9:09 PM] AlphaGalleon Health Monitor triggered.
Status: ⚠️ Upstox API is returning MOCK data (Token may be invalid).
Action: Surfacing to Arjun despite the strict cron filter, as mock data indicates a break in live services.
Found that `backend/services/upstox_service.py` returns mock data even on 200 if the token is missing or call fails, which prevents the monitor script from hitting the 'TOKEN_EXPIRED' error state (401).
